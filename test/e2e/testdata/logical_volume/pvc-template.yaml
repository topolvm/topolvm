kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: %s
spec:
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: %d
  storageClassName: topolvm-provisioner-immediate
