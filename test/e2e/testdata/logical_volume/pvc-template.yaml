kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: %s
spec:
  volumeMode: Filesystem
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: %dMi
  storageClassName: topolvm-provisioner-immediate
