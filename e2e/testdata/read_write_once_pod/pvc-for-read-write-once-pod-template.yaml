kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: %s
  namespace: %s
spec:
  accessModes:
  - ReadWriteOncePod
  resources:
    requests:
      storage: %s
  storageClassName: topolvm-provisioner-default
